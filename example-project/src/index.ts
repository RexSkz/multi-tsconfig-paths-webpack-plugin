/* eslint-disable no-console, import/no-unresolved, import/order */
import { rootValue } from '@/foo';
import { userValue } from './domains/user/src';

console.log('rootValue', rootValue);
console.log('userValue', userValue);
